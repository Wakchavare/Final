{
  "name": "casting-production-management",
  "version": "1.0.0",
  "private": true,
  "description": "Casting Production Management with Express APIs and PostgreSQL.",
  "scripts": {
    "build": "node --check src/env.js && node --check src/server.js && node --check src/seed.js && node --check src/reset-db.js",
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js",
    "reset-db": "node src/reset-db.js",
    "test": "node --test tests/*.test.js",
    "browser-smoke": "node scripts/browser-smoke-test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
